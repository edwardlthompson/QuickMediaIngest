<Window x:Class="QuickMediaIngest.ShortcutsHelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:QuickMediaIngest.Localization"
        Title="{loc:Loc Help_ShortcutsTitle}" Width="440" Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">
    <ScrollViewer Padding="16">
        <StackPanel>
            <TextBlock Text="{loc:Loc Help_ShortcutsIntro}" Style="{StaticResource AppBodyTextStyle}" TextWrapping="Wrap" Margin="0,0,0,12"/>
            <ItemsControl ItemsSource="{Binding Rows, RelativeSource={RelativeSource AncestorType=Window}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Key}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Description}" TextWrapping="Wrap"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Content="{loc:Loc Btn_Close}" Margin="0,16,0,0" HorizontalAlignment="Right" Padding="16,8" Click="Close_Click"/>
        </StackPanel>
    </ScrollViewer>
</Window>
