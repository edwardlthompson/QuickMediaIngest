<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Reference typography dictionary -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Themes/Typography.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- ============ CONTROL BLOCK CONTAINER ============ -->
    <!-- Used to group related controls together -->
    
    <Style x:Key="Theme.ControlBlockBorder" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Theme.CardBackground.Brush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Brush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="3"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
    </Style>

    <!-- ============ BUTTON STYLES ============ -->
    
    <Style x:Key="Theme.PrimaryButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Background" Value="{DynamicResource Theme.Accent.Brush}"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary.Brush}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="0,0,4,0"/>
    </Style>

    <Style x:Key="Theme.SecondaryButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
        <Setter Property="Foreground" Value="{DynamicResource Theme.Accent.Brush}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="0,0,4,0"/>
    </Style>

    <!-- ============ CHECKBOX STYLES ============ -->
    
    <Style x:Key="Theme.CheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextSecondary.Brush}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontFamily" Value="{StaticResource Theme.FontFamilyDefault}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
    </Style>

    <!-- ============ TEXTBOX STYLES ============ -->
    
    <Style x:Key="Theme.TextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextSecondary.Brush}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontFamily" Value="{StaticResource Theme.FontFamilyDefault}"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ============ ICON BUTTON ============ -->
    
    <Style x:Key="Theme.ToggleButton" TargetType="ToggleButton" BasedOn="{StaticResource MaterialDesignActionToggleButton}">
        <Setter Property="Margin" Value="4"/>
    </Style>

</ResourceDictionary>
