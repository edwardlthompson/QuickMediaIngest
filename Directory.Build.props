<Project>
  <PropertyGroup>
    <!-- Allow restore/compile of net8.0-windows on Linux CI/agents. Runtime still requires Windows. -->
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
  </PropertyGroup>
</Project>
